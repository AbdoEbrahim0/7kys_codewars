//methode 1
int maxMultiple(int divisor, int bound) 
{
  for (int i = bound  ; i >=1 ; i--)
    {
    if(i % divisor==0)
      return i;
  }
  return 0;
}
//methode 2
int maxMultiple(int divisor, int bound) {
  return bound-bound%divisor;
}
